const initialState = {
    friends: [
        {id: 1, name: "Kirill"},
        {id: 2, name: "Svetlana"},
        {id: 3, name: "Iosif"},
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
};

export default sidebarReducer;